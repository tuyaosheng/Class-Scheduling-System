# 年级日历参数化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `scheduler/core/calendar.py` 里写死的全局 5 天×9 节常量，改成按年级读取的 `GradeCalendar`，让 `compiler`/`verifier`/`precheck`/`importer`/`exporter`/`ruletext`/`cli` 都从 `Dataset.calendar` 取天数/节数/午休边界，而不是模块级常量。

**Architecture:** 新增 `GradeCalendar` pydantic 模型（把现在 `calendar.py` 的自由函数原样搬成方法），挂在 `Dataset.calendar` 字段上。`Dataset.calendar` 给一个匹配现有全局常量的 `default_factory`，这样所有没有显式传 `calendar=` 的现有测试构造调用保持完全不变的行为，不需要批量改测试。生产代码路径（`importer.py` 构造 `Dataset`）显式传入 `cfg.calendar_of(grade)`，从新增的 `calendars.yaml` 读取。`scheduler/core/calendar.py` 的全局常量**不删除**——继续作为默认 `GradeCalendar` 的数据来源，也是这次改动能把测试迁移成本降到最低的关键。

**Tech Stack:** Python 3.10、pydantic 2、pytest、PyYAML

**Spec:** `docs/superpowers/specs/2026-08-23-年级日历参数化-design.md`

## Global Constraints

- `compiler.py` 与 `verifier.py` 不得共享任何约束逻辑代码（铁律 4，见 `CLAUDE.md`）——两边各自独立把 `cal.*` 换成 `calendar.*`，不要抽公共函数。
- 每完成一个文件的迁移就跑一次该文件对应的现有测试，全绿才能进下一个任务；不要攒到最后一次性调试。
- `scheduler/core/calendar.py` 的模块级常量（`DAYS`/`PERIODS_PER_DAY`/`N_SLOTS`/`MORNING`/`AFTERNOON`）本次全部保留，不删除、不改值。

---

## Task 1: `GradeCalendar` 模型 + `Dataset.calendar` 字段

**Files:**
- Modify: `scheduler/core/models.py`
- Test: `tests/test_models_config.py`

**Interfaces:**
- Produces: `GradeCalendar`（字段 `days: List[str]`、`periods_per_day: int`、`midday_break_after: int`、`clock_times: Optional[List[Tuple[str, str]]] = None`、`reserved_slots: List[dict] = []`；方法 `day_index(name) -> int`、`slot_index(day, period) -> int`、`slot_of(index) -> Tuple[int, int]`、`section_period(section, n) -> int`、`adjacent_pairs() -> List[Tuple[int, int]]`；属性 `n_slots`、`morning`、`afternoon`）
- Produces: `Dataset.calendar: GradeCalendar`，未显式传入时默认等价于现在 `calendar.py` 的 5 天×9 节、午休 5/6 节

- [ ] **Step 1: 写失败的测试**

在 `tests/test_models_config.py` 末尾加：

```python
def test_grade_calendar_slot_round_trip():
    from scheduler.core.models import GradeCalendar
    calendar = GradeCalendar(
        days=['周一', '周二', '周三', '周四', '周五'],
        periods_per_day=9,
        midday_break_after=5,
    )
    assert calendar.n_slots == 45
    assert calendar.slot_index(1, 8) == 17
    assert calendar.slot_of(17) == (1, 8)
    assert calendar.day_index('周三') == 2
    assert calendar.morning == range(1, 6)
    assert calendar.afternoon == range(6, 10)
    assert (5, 6) not in calendar.adjacent_pairs()
    assert (4, 5) in calendar.adjacent_pairs()


def test_grade_calendar_section_period():
    from scheduler.core.models import GradeCalendar
    calendar = GradeCalendar(days=['周一'], periods_per_day=9, midday_break_after=5)
    assert calendar.section_period('上午', 3) == 3
    assert calendar.section_period('下午', 2) == 7
    assert calendar.section_period(None, 9) == 9
    with pytest.raises(ValueError):
        calendar.section_period('上午', 6)


def test_dataset_calendar_defaults_to_current_global_shape():
    from scheduler.core.models import Dataset, Teacher, TeachingTask
    task = TeachingTask(id=0, grade='初三', class_id=1, course='语文', teacher='A', periods=1)
    ds = Dataset(grade='初三', classes=[1], teachers={'A': Teacher(name='A')}, tasks=[task])
    assert ds.calendar.n_slots == 45
    assert ds.calendar.periods_per_day == 9
    assert ds.calendar.midday_break_after == 5
```

`tests/test_models_config.py` 顶部需要 `import pytest`（如果还没有）。

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_models_config.py -k grade_calendar -v`
Expected: FAIL，`ImportError: cannot import name 'GradeCalendar'`

- [ ] **Step 3: 实现 `GradeCalendar` 与 `Dataset.calendar`**

在 `scheduler/core/models.py` 顶部 `from typing import Dict, List, Optional` 改成：

```python
from typing import Dict, List, Optional, Tuple
```

在 `Course` 类之前插入：

```python
def _default_grade_calendar() -> 'GradeCalendar':
    """匹配 calendar.py 现有全局常量的默认日历——5 天、9 节、午休 5/6 节间。

    这是本次年级参数化改动的关键兼容手段：所有没有显式传 calendar= 的
    现有 Dataset(...) 构造调用（尤其是测试里的 make_dataset/ds 辅助函数）
    自动获得和改动前完全一致的行为，不需要批量修改测试。
    """
    return GradeCalendar(
        days=['周一', '周二', '周三', '周四', '周五'],
        periods_per_day=9,
        midday_break_after=5,
    )


class GradeCalendar(BaseModel):
    """一个年级的时间格形状：天数、每天节数、午休边界。

    slot_index/slot_of 的换算公式依赖 periods_per_day，一旦按年级不同就
    不能再是全局函数——这是把 calendar.py 的自由函数改成本类方法的原因。
    """
    days: List[str]
    periods_per_day: int
    midday_break_after: int
    clock_times: Optional[List[Tuple[str, str]]] = None
    reserved_slots: List[dict] = Field(default_factory=list)

    @property
    def n_slots(self) -> int:
        return len(self.days) * self.periods_per_day

    @property
    def morning(self) -> range:
        return range(1, self.midday_break_after + 1)

    @property
    def afternoon(self) -> range:
        return range(self.midday_break_after + 1, self.periods_per_day + 1)

    def day_index(self, name: str) -> int:
        try:
            return self.days.index(name)
        except ValueError:
            raise ValueError('未知星期：%r，仅支持 %s' % (name, self.days))

    def slot_index(self, day: int, period: int) -> int:
        if not 0 <= day < len(self.days):
            raise ValueError('星期序号越界：%s' % day)
        if not 1 <= period <= self.periods_per_day:
            raise ValueError('节次越界：%s' % period)
        return day * self.periods_per_day + (period - 1)

    def slot_of(self, index: int) -> Tuple[int, int]:
        if not 0 <= index < self.n_slots:
            raise ValueError('时间格索引越界：%s' % index)
        return index // self.periods_per_day, index % self.periods_per_day + 1

    def section_period(self, section: Optional[str], n: int) -> int:
        if section == '上午':
            if n not in self.morning:
                raise ValueError('上午只有 1-%d 节，收到第 %s 节' % (self.midday_break_after, n))
            return n
        if section == '下午':
            afternoon_len = self.periods_per_day - self.midday_break_after
            if not 1 <= n <= afternoon_len:
                raise ValueError('下午只有 1-%d 节，收到第 %s 节' % (afternoon_len, n))
            return self.midday_break_after + n
        if section is None:
            if not 1 <= n <= self.periods_per_day:
                raise ValueError('节次越界：%s' % n)
            return n
        raise ValueError('未知时段：%r' % section)

    def adjacent_pairs(self) -> List[Tuple[int, int]]:
        """一天之内可构成连堂的节次对，排除午休边界。"""
        return [(p, p + 1) for p in range(1, self.periods_per_day)
                if p != self.midday_break_after]
```

`models.py` 顶部的 `from pydantic import BaseModel, Field` 要确认已经 import 了 `Field`（现有 `Teacher.duties`/`Teacher.forbidden` 已经用了 `Field(default_factory=list)`，应该已经 import 过）。

然后在 `Dataset` 类里加字段（`Dataset` 定义在文件末尾，`tasks: List[TeachingTask]` 那一行之后）：

```python
class Dataset(BaseModel):
    """一次求解的全部输入。"""
    grade: str
    classes: List[int]
    teachers: Dict[str, Teacher]
    tasks: List[TeachingTask]
    calendar: GradeCalendar = Field(default_factory=_default_grade_calendar)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_models_config.py -v`
Expected: PASS，包括新加的三个测试和原有全部测试

- [ ] **Step 5: 跑全量测试确认没有连带破坏**

Run: `python -m pytest -q`
Expected: PASS（这一步只新增了字段和默认值，不改任何消费方代码，理论上不会破坏任何现有测试；如果破坏了说明默认值算错了，回头检查 `_default_grade_calendar`）

- [ ] **Step 6: Commit**

```bash
git add scheduler/core/models.py tests/test_models_config.py
git commit -m "feat: 新增 GradeCalendar 模型，Dataset 挂载按年级日历字段"
```

---

## Task 2: `calendars.yaml` + `SchedulerConfig.calendar_of`

**Files:**
- Create: `scheduler/config/calendars.yaml`
- Modify: `scheduler/core/config.py`
- Test: `tests/test_models_config.py`

**Interfaces:**
- Consumes: `GradeCalendar`（Task 1）
- Produces: `SchedulerConfig.calendars: Dict[str, GradeCalendar]`、`SchedulerConfig.calendar_of(grade: str) -> GradeCalendar`

- [ ] **Step 1: 新建 `calendars.yaml`**

`scheduler/config/calendars.yaml`：

```yaml
grade_calendars:
  初三:
    days: [周一, 周二, 周三, 周四, 周五]
    periods_per_day: 9
    midday_break_after: 5
    clock_times:
      - ["08:00", "08:40"]
      - ["09:15", "09:55"]
      - ["10:05", "10:43"]
      - ["10:53", "11:31"]
      - ["11:40", "12:20"]
      - ["14:20", "15:00"]
      - ["15:15", "15:55"]
      - ["16:10", "16:50"]
      - ["16:50", "17:30"]
    reserved_slots:
      - {day: 0, period: 9, label: 班会}
      - {day: 1, period: 8, label: 体比}
      - {day: 1, period: 9, label: 体比}
      - {day: 2, period: 8, label: 校本1}
      - {day: 2, period: 9, label: 校本1}
      - {day: 3, period: 8, label: 综实2}
      - {day: 3, period: 9, label: 综实2}
      - {day: 4, period: 9, label: 体选}
```

（这里先用"初三"这个 key，不改成"九年级"——年级叫法统一是一件独立的、机械的收尾工作，见 `docs/superpowers/specs/2026-08-23-课程配置扩展与跨年级防冲突-design.md`，本计划不做，避免和这次的架构改动混在一起。）

- [ ] **Step 2: 写失败的测试**

在 `tests/test_models_config.py` 加：

```python
def test_config_loads_calendars(cfg):
    calendar = cfg.calendar_of('初三')
    assert calendar.periods_per_day == 9
    assert calendar.midday_break_after == 5
    assert len(calendar.reserved_slots) == 8


def test_calendar_of_unknown_grade_raises(cfg):
    with pytest.raises(ConfigError):
        cfg.calendar_of('不存在的年级')
```

- [ ] **Step 3: 跑测试确认失败**

Run: `python -m pytest tests/test_models_config.py -k calendar_of -v`
Expected: FAIL，`AttributeError: 'SchedulerConfig' object has no attribute 'calendar_of'`

- [ ] **Step 4: 实现 `SchedulerConfig.calendars`/`calendar_of`**

`scheduler/core/config.py` 顶部 `from .models import Course, Venue` 改成：

```python
from .models import Course, GradeCalendar, Venue
```

`SchedulerConfig` 类里 `reserved_slots` 字段之后加：

```python
    calendars: Dict[str, GradeCalendar] = {}

    def calendar_of(self, grade: str) -> GradeCalendar:
        try:
            return self.calendars[grade]
        except KeyError:
            raise ConfigError('年级 %r 没有对应的日历配置（calendars.yaml）' % grade)
```

`load_config` 函数里，`plans = _read(...)` 那几行之后加载 `calendars.yaml`：

```python
    calendars_path = config_dir / 'calendars.yaml'
    calendars = {}
    if calendars_path.exists():
        raw_calendars = yaml.safe_load(calendars_path.read_text(encoding='utf-8')) or {}
        calendars = {g: GradeCalendar(**c) for g, c in (raw_calendars.get('grade_calendars') or {}).items()}
```

`SchedulerConfig(...)` 构造那一行加上 `calendars=calendars`：

```python
    cfg = SchedulerConfig(courses=courses, plans={g: (p or {}) for g, p in plans.items()},
                          venues=venues, reserved_slots=reserved_slots, calendars=calendars)
```

- [ ] **Step 5: 跑测试确认通过**

Run: `python -m pytest tests/test_models_config.py -v`
Expected: PASS

- [ ] **Step 6: 跑全量测试**

Run: `python -m pytest -q`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add scheduler/config/calendars.yaml scheduler/core/config.py tests/test_models_config.py
git commit -m "feat: calendars.yaml + SchedulerConfig.calendar_of"
```

---

## Task 3: `ruletext.py` 按年级日历解析

**Files:**
- Modify: `scheduler/core/ruletext.py`
- Test: `tests/test_ruletext_forbid.py`、`tests/test_ruletext_misc.py`

**Interfaces:**
- Consumes: `GradeCalendar`（Task 1）
- Produces: `parse_time_expr(text, calendar)`、`parse_fixed_slots(text, calendar)`（原来是 `parse_time_expr(text)`/`parse_fixed_slots(text)`，签名变了，调用方要跟着改——见 Task 5）

- [ ] **Step 1: 写失败的测试**

`tests/test_ruletext_forbid.py` 和 `tests/test_ruletext_misc.py` 都调了 `parse_time_expr`/`parse_fixed_slots`（大概率是 `parse_time_expr('周二第4节')` 这种一个参数的调用），本步骤在两个文件里把这些调用全部改成两参数。两个文件顶部各加：

```python
from scheduler.core.models import GradeCalendar

CAL = GradeCalendar(days=['周一', '周二', '周三', '周四', '周五'],
                    periods_per_day=9, midday_break_after=5)
```

把两个文件里所有 `parse_time_expr(text)` 改成 `parse_time_expr(text, CAL)`，所有 `parse_fixed_slots(text)` 改成 `parse_fixed_slots(text, CAL)`。

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_ruletext_forbid.py tests/test_ruletext_misc.py -v`
Expected: FAIL，`TypeError: parse_time_expr() takes 1 positional argument but 2 were given`

- [ ] **Step 3: 实现**

`scheduler/core/ruletext.py` 里 `from . import calendar as cal` 这行删掉（不再需要模块级 import，改成参数传入）。

```python
def parse_time_expr(text, calendar):
    """解析禁排/固定节次文本，返回 {(day_index, period)} 集合。"""
    if not text or not str(text).strip():
        return set()
    text = str(text).strip()
    slots = set()
    for weekday, body in _split_clauses(text):
        day = calendar.day_index(weekday)
        section = '上午' if '上午' in body else ('下午' if '下午' in body else None)
        numbers = [int(n) for n in _DIGITS_RE.findall(body)]
        if numbers:
            for n in numbers:
                try:
                    slots.add((day, calendar.section_period(section, n)))
                except ValueError as exc:
                    raise RuleTextError('%s%r: %s' % (weekday, body, exc)) from exc
        elif section == '上午':
            slots.update((day, p) for p in calendar.morning)
        elif section == '下午':
            slots.update((day, p) for p in calendar.afternoon)
        else:
            slots.update((day, p) for p in range(1, calendar.periods_per_day + 1))
    return slots
```

```python
def parse_fixed_slots(text, calendar):
    """解析「固定节次」列。语义是窗口：在这些格里排完该课的周课时。"""
    return parse_time_expr(text, calendar)
```

`parse_requirement`/`parse_remark` 不改——它们不引用 `cal`。

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_ruletext_forbid.py tests/test_ruletext_misc.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add scheduler/core/ruletext.py tests/test_ruletext_forbid.py tests/test_ruletext_misc.py
git commit -m "refactor: ruletext 解析函数按年级日历参数化"
```

---

## Task 4: `importer.py` 挂载 `Dataset.calendar`

**Files:**
- Modify: `scheduler/core/importer.py`
- Test: `tests/test_importer.py`（`import_excel` 单文件路径）、`tests/test_importer_merge.py`（`merge_teaching_and_rules` 两文件路径）

**Interfaces:**
- Consumes: `cfg.calendar_of(grade)`（Task 2）、`parse_time_expr(text, calendar)`/`parse_fixed_slots(text, calendar)`（Task 3）
- Produces: `import_excel`/`merge_teaching_and_rules` 返回的 `Dataset` 都带正确的 `calendar` 字段

- [ ] **Step 1: 写失败的测试**

在 `tests/test_importer.py` 里加（用文件里已有的 `CONFIG_DIR`/`EXCEL`/`load_config`/`import_excel` 导入方式，不要新建一套）：

```python
def test_imported_dataset_has_grade_calendar():
    cfg = load_config(CONFIG_DIR)
    result = import_excel(EXCEL, cfg, grade='初三')
    assert result.dataset.calendar.periods_per_day == 9
    assert result.dataset.calendar is cfg.calendar_of('初三')
```

在 `tests/test_importer_merge.py` 里加（同样对齐文件里已有的 fixture 风格，`merge_teaching_and_rules` 需要 `teaching_path`/`rules_path` 两份文件，用文件里已有的测试数据 fixture）：

```python
def test_merged_dataset_has_grade_calendar():
    cfg = load_config(CONFIG_DIR)
    result = merge_teaching_and_rules(TEACHING_XLSX, RULES_XLSX, cfg, grade='初三')
    assert result.dataset.calendar.periods_per_day == 9
    assert result.dataset.calendar is cfg.calendar_of('初三')
```

（`TEACHING_XLSX`/`RULES_XLSX` 换成文件里实际用的 fixture 变量名——先读一遍 `tests/test_importer_merge.py` 现有测试怎么调 `merge_teaching_and_rules`，照抄它的参数来源，不要另起一套数据。）

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_importer.py tests/test_importer_merge.py -k grade_calendar -v`
Expected: FAIL，`AttributeError` 或断言失败（此时 `dataset.calendar` 是 Task 1 的默认值，不是 `cfg.calendar_of('初三')` 返回的对象）

- [ ] **Step 3: 实现 —— `import_excel`（CLI 用的单文件路径）**

`scheduler/core/importer.py` 里 `import_excel(path, cfg, grade='初三')` 函数开头（`wb = openpyxl.load_workbook(...)` 之前）加：

```python
    calendar = cfg.calendar_of(grade)
```

第一遍聚合教师禁排那行（原来是 `forbidden[name] |= parse_time_expr(_cell(row, '不能排课节次'))`）：

```python
        forbidden[name] |= parse_time_expr(_cell(row, '不能排课节次'), calendar)
```

`Dataset(...)` 构造那行（原来是 `dataset = Dataset(grade=grade, classes=sorted(classes), teachers=teachers, tasks=tasks)`）：

```python
    dataset = Dataset(grade=grade, classes=sorted(classes),
                      teachers=teachers, tasks=tasks, calendar=calendar)
```

`_build_rules(rows, cfg, grade, forbidden)` 签名改成 `_build_rules(rows, cfg, grade, forbidden, calendar)`，函数体内固定节次那行（原来是 `slots = parse_fixed_slots(_cell(row, '固定节次'))`）：

```python
        slots = parse_fixed_slots(_cell(row, '固定节次'), calendar)
```

调用处（`import_excel` 函数体里）`rules = _build_rules(rows, cfg, grade, forbidden)` 改成 `rules = _build_rules(rows, cfg, grade, forbidden, calendar)`。

- [ ] **Step 3b: 实现 —— `merge_teaching_and_rules`（Web API 用的两文件路径）**

这是 `routes.py` 的 `/api/import` 实际调用的函数，和 `import_excel` 是两条独立代码路径，都要改。

函数开头 `teaching_pivot = parse_teaching_table(teaching_path, cfg)` 之前加：

```python
    calendar = cfg.calendar_of(grade)
```

教师禁排聚合那行（原来是 `forbidden[name] |= parse_time_expr(_cell(row, '不能排课节次'))`，在 `if rule_engine != 'ai':` 分支里）：

```python
            forbidden[name] |= parse_time_expr(_cell(row, '不能排课节次'), calendar)
```

正则引擎解析固定节次那行（原来是 `fixed_slots = parse_fixed_slots(fixed_raw)`）：

```python
            fixed_slots = parse_fixed_slots(fixed_raw, calendar)
```

规则回显那处解析禁排原文用于展示（原来是 `else parse_time_expr(not_avail_raw)`，在三元表达式里）：

```python
            not_avail_display_slots = ({(d, p) for d, p in parsed.not_available} if rule_engine == 'ai'
                                       else parse_time_expr(not_avail_raw, calendar))
```

`Dataset(...)` 构造那行（原来是 `dataset = Dataset(grade=grade, classes=sorted(classes), teachers=teachers, tasks=tasks)`）：

```python
    dataset = Dataset(grade=grade, classes=sorted(classes), teachers=teachers,
                      tasks=tasks, calendar=calendar)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_importer.py tests/test_importer_merge.py -v`
Expected: PASS

- [ ] **Step 5: 跑全量测试**

Run: `python -m pytest -q`
Expected: PASS（`importer.py` 是生产路径第一次真正传入非默认 calendar，但九年级/初三的 calendar 内容和默认值完全一致，不应该有任何行为变化）

- [ ] **Step 6: Commit**

```bash
git add scheduler/core/importer.py tests/test_importer.py tests/test_importer_merge.py
git commit -m "feat: importer 构造 Dataset 时挂载年级日历"
```

---

## Task 5: `compiler.py` 按年级日历编译

**Files:**
- Modify: `scheduler/core/compiler.py`
- Test: `tests/test_compiler_base.py`、`tests/test_compiler_alternate.py`、`tests/test_compiler_daily.py`、`tests/test_compiler_slots.py`、`tests/test_rules_scope.py`、`tests/test_teacher_max_run.py`

**Interfaces:**
- Consumes: `c.dataset.calendar`（`CompiledModel.dataset` 已经存在，Task 1 让它带 `calendar`）

- [ ] **Step 1: 确认现有测试作为回归基线**

Run: `python -m pytest tests/test_compiler_base.py tests/test_compiler_alternate.py tests/test_compiler_daily.py tests/test_compiler_slots.py tests/test_rules_scope.py tests/test_teacher_max_run.py -v`
Expected: 全部 PASS（改动前的基线，本任务不新增测试用例，靠这些已有测试原样通过来验证迁移没有改变行为）

- [ ] **Step 2: 替换所有 `cal.` 引用**

`scheduler/core/compiler.py` 顶部 `from . import calendar as cal` 保留（`NO_ADJACENT`/`adjacent_pairs()` 等旧写法会被下面的改动整个替换掉，模块级 import 不再被用到时可以删，但先保留不影响正确性，最后统一检查）。

把下面这些函数体内的 `cal.N_SLOTS` 全部替换成 `c.dataset.calendar.n_slots`（`compile_model`/`_add_period_counts`/`_add_class_no_clash`/`_add_teacher_no_clash`/`_compile_forbid_slots`/`_compile_pin_window`/`_add_daily`/`_compile_alternate_weeks`/`_limit_venue` 里各有一到多处 `range(cal.N_SLOTS)` 或 `for slot in range(cal.N_SLOTS)`）：

```python
def compile_model(dataset, cfg, rules, *, with_assumptions=False) -> CompiledModel:
    model = cp_model.CpModel()
    x = {(t.id, s): model.NewBoolVar('x_%d_%d' % (t.id, s))
         for t in dataset.tasks for s in range(dataset.calendar.n_slots)}
    compiled = CompiledModel(model, x, dataset, cfg)
    ...
```

（`compile_model` 内部后续对 `range(cal.N_SLOTS)` 的引用同理替换成 `range(compiled.dataset.calendar.n_slots)` 或在函数体里先取 `calendar = dataset.calendar` 局部变量再用 `calendar.n_slots`——两种写法都行，选一种在整个文件里保持一致，这里选后者，减少重复的 `.dataset.calendar` 链式访问。）

给 `CompiledModel` 加一个方便属性（在 `task_vars` 方法旁边）：

```python
    @property
    def calendar(self):
        return self.dataset.calendar
```

这样后续所有函数体内把 `cal.N_SLOTS` 换成 `c.calendar.n_slots`，`cal.DAYS` 换成 `c.calendar.days`，`cal.PERIODS_PER_DAY` 换成 `c.calendar.periods_per_day`，`cal.slot_index(day, p)` 换成 `c.calendar.slot_index(day, p)`，比每次都写 `c.dataset.calendar` 短。

逐个函数过一遍（对照当前文件实际内容替换，函数名不变，只换取值来源）：

- `_add_period_counts`：不涉及 `cal.`，跳过
- `_add_class_no_clash`：`for slot in range(cal.N_SLOTS)` → `for slot in range(c.calendar.n_slots)`
- `_add_teacher_no_clash`：同上
- `_compile_forbid_slots`：`_slot_set(rule)` 不涉及 calendar，跳过；本函数体 `for task in select_tasks(...)` 循环里没有 `cal.`，跳过
- `_compile_pin_window`：`for slot in range(cal.N_SLOTS)` → `for slot in range(c.calendar.n_slots)`
- `_slots_of_day(day)` 函数签名加 `calendar` 参数：`def _slots_of_day(day, calendar): return [calendar.slot_index(day, p) for p in range(1, calendar.periods_per_day + 1)]`，调用处 `_slots_of_day(day)` 改成 `_slots_of_day(day, c.calendar)`（在 `_add_daily` 里）
- `_add_daily`：`days = [cal.day_index(d) for d in weekdays] if weekdays else range(len(cal.DAYS))` 改成 `days = [c.calendar.day_index(d) for d in weekdays] if weekdays else range(len(c.calendar.days))`；调用 `_slots_of_day(day)` 改成 `_slots_of_day(day, c.calendar)`
- `adjacent_pairs()` 自由函数**删除**，改成直接调用 `c.calendar.adjacent_pairs()`（`NO_ADJACENT` 常量也删除，逻辑已经并进 `GradeCalendar.adjacent_pairs()`）
- `_compile_consecutive`：`pairs = adjacent_pairs()` 改成 `pairs = c.calendar.adjacent_pairs()`；`for day in range(len(cal.DAYS))` 改成 `for day in range(len(c.calendar.days))`；`if run_periods[-1] > cal.PERIODS_PER_DAY` 改成 `if run_periods[-1] > c.calendar.periods_per_day`；`cal.slot_index(_day, period)` 改成 `c.calendar.slot_index(_day, period)`
- `_limit_venue`：`for slot in range(cal.N_SLOTS)` → `for slot in range(c.calendar.n_slots)`
- `_halfday_run_starts(length=3)` 签名改成 `_halfday_run_starts(calendar, length=3)`（`calendar` 挪到第一个无默认值的位置，`length` 保留默认值）：`for half in (cal.MORNING, cal.AFTERNOON)` 改成 `for half in (calendar.morning, calendar.afternoon)`；调用处（`_compile_teacher_max_run` 里）`starts = _halfday_run_starts(window_len)` 改成 `starts = _halfday_run_starts(c.calendar, window_len)`
- `_compile_teacher_max_run`：`for day in range(len(cal.DAYS))` 改成 `for day in range(len(c.calendar.days))`；`cal.slot_index(day, pp)` 改成 `c.calendar.slot_index(day, pp)`

改完在文件顶部把 `from . import calendar as cal` 删掉（确认没有遗漏引用后再删——先跑一次 `grep -n "cal\." scheduler/core/compiler.py`，应该没有输出）。

- [ ] **Step 3: 跑测试确认通过**

Run: `python -m pytest tests/test_compiler_base.py tests/test_compiler_alternate.py tests/test_compiler_daily.py tests/test_compiler_slots.py tests/test_rules_scope.py tests/test_teacher_max_run.py -v`
Expected: PASS，且断言内容和 Step 1 的基线完全一致（这次改动不应该让任何测试的行为改变，只是取值来源换了）

- [ ] **Step 4: 跑全量测试**

Run: `python -m pytest -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add scheduler/core/compiler.py
git commit -m "refactor: compiler.py 按年级日历编译，不再用全局 calendar 常量"
```

---

## Task 6: `verifier.py` 按年级日历校验

**Files:**
- Modify: `scheduler/core/verifier.py`
- Test: `tests/test_verifier.py`、`tests/test_solve_e2e.py`

**Interfaces:**
- Consumes: `dataset.calendar`（`verify`/`precheck` 等顶层函数已经接收 `dataset` 参数）

**铁律 4 提醒**：本任务和 Task 5 各自独立完成，不要把 `compiler.py` 改完的辅助函数拿过来复用。

- [ ] **Step 1: 确认现有测试作为回归基线**

Run: `python -m pytest tests/test_verifier.py tests/test_solve_e2e.py -v`
Expected: 全部 PASS

- [ ] **Step 2: 替换所有 `cal.` 引用**

逐函数过一遍 `scheduler/core/verifier.py`（对照实际当前内容替换）：

- `_check_period_counts`、`_check_class_clash` 等不涉及 `cal.` 的函数跳过
- 所有 `cal.DAYS[day]`（出现在各 `Violation(detail=...)` 拼字符串的地方，如 `_check_class_clash`/`_check_teacher_clash`/`_venue_overflow` 等）需要能拿到 `calendar`——这些函数目前只收 `placements`（部分）或 `placements, cfg`，要在调用链路上补一个 `calendar` 参数，从顶层 `verify(solution, dataset, cfg, rules)` 往下传
- `verify(solution, dataset, cfg, rules)`：函数体第一行加 `calendar = dataset.calendar`，后续调用 `_check_class_clash(placements)` 改成 `_check_class_clash(placements, calendar)`，`_check_teacher_clash(placements)` 改成 `_check_teacher_clash(placements, calendar)`，`_check_venues(placements, cfg)` 改成 `_check_venues(placements, cfg, calendar)`
- `_check_class_clash(placements, calendar)`：内部 `cal.slot_of(slot)` 改成 `calendar.slot_of(slot)`，`cal.DAYS[day]` 改成 `calendar.days[day]`
- `_check_teacher_clash(placements, calendar)`：同上
- `_venue_overflow(placements, cfg, venue_name, capacity, calendar)`：签名加 `calendar`，内部 `cal.slot_of(slot)`/`cal.DAYS[day]` 同上替换；调用处 `_check_venues` 往下传
- `_check_venues(placements, cfg, calendar)`：内部调 `_venue_overflow(..., calendar)`
- 各 `_RULE_CHECKS` 对应的函数（`_check_daily_min`/`_check_daily_max`/`_check_consecutive`/`_check_alternate`/`_check_venue_capacity`/`_check_teacher_max_run`）：这些函数签名统一是 `(placements, dataset, cfg, rule)`，`dataset` 已经在手，函数体内把 `cal.xxx` 换成 `dataset.calendar.xxx` 即可，不需要额外加参数
- `_check_teacher_max_run`：`cal.slot_of(p.slot)` → `dataset.calendar.slot_of(p.slot)`；`for half, half_label in ((cal.MORNING, '上午'), (cal.AFTERNOON, '下午'))` → `for half, half_label in ((dataset.calendar.morning, '上午'), (dataset.calendar.afternoon, '下午'))`；`cal.DAYS[day]` → `dataset.calendar.days[day]`

确认没有遗漏：`grep -n "cal\." scheduler/core/verifier.py` 应该没有输出，然后删掉顶部 `from . import calendar as cal`。

- [ ] **Step 3: 跑测试确认通过**

Run: `python -m pytest tests/test_verifier.py tests/test_solve_e2e.py -v`
Expected: PASS，断言内容和 Step 1 基线一致

- [ ] **Step 4: 跑全量测试**

Run: `python -m pytest -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add scheduler/core/verifier.py
git commit -m "refactor: verifier.py 按年级日历校验，不再用全局 calendar 常量"
```

---

## Task 7: `precheck.py` 按年级日历预检

**Files:**
- Modify: `scheduler/core/precheck.py`
- Test: `tests/test_precheck.py`

- [ ] **Step 1: 确认现有测试作为回归基线**

Run: `python -m pytest tests/test_precheck.py -v`
Expected: 全部 PASS

- [ ] **Step 2: 替换 `cal.N_SLOTS` 引用**

`_check_teacher_capacity(dataset, cfg)`：`available = cal.N_SLOTS - blocked` 改成 `available = dataset.calendar.n_slots - blocked`；`% (name, needed, available, cal.N_SLOTS, blocked, needed - available)` 改成用 `dataset.calendar.n_slots`。

`_check_class_capacity(dataset, cfg)`：`available = cal.N_SLOTS - len(...)` 同样改成 `dataset.calendar.n_slots`。

`_check_venue_capacity(dataset, cfg)`：`supply = venue.capacity * cal.N_SLOTS` 改成 `supply = venue.capacity * dataset.calendar.n_slots`；对应的格式化字符串里的 `cal.N_SLOTS` 一并替换。

确认无遗漏后删掉 `from . import calendar as cal`。

- [ ] **Step 3: 跑测试确认通过**

Run: `python -m pytest tests/test_precheck.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add scheduler/core/precheck.py
git commit -m "refactor: precheck.py 按年级日历校验容量"
```

---

## Task 8: `exporter.py` 按年级日历导出

**Files:**
- Modify: `scheduler/core/exporter.py`
- Test: `tests/test_exporter_template.py`（教务模板版导出）、`tests/test_solve_e2e.py`（简单网格版 `export_excel` 只在这里被间接测到，没有独立的 exporter 测试文件）

**已知范围限制（不在本任务解决）**：`export_to_template` 仍然只能配一份模板文件；不同 `periods_per_day` 的年级需要各自的模板文件才能正确导出，这个连带问题记在 `docs/superpowers/specs/2026-08-23-年级日历参数化-design.md` 第 6.1 节，本任务只保证代码从 `dataset.calendar` 取值而不是硬编码全局常量，不解决"一份模板打天下"这件事本身。

- [ ] **Step 1: 确认现有测试作为回归基线**

Run: `python -m pytest tests/test_exporter_template.py tests/test_solve_e2e.py -v`
Expected: 全部 PASS

- [ ] **Step 2: 替换所有 `cal.` 引用**

`_write_grid(ws, columns, cell_text, reserved_slots=())` 签名加 `calendar` 参数：

```python
def _write_grid(ws, columns, cell_text, calendar, reserved_slots=()):
    ws.cell(row=1, column=1, value='星期').font = Font(bold=True)
    ws.cell(row=1, column=2, value='节次').font = Font(bold=True)
    for col, name in enumerate(columns, start=3):
        ws.cell(row=1, column=col, value=name).font = Font(bold=True)
    for slot in range(calendar.n_slots):
        day, period = calendar.slot_of(slot)
        row = slot + 2
        ws.cell(row=row, column=1, value=calendar.days[day])
        ws.cell(row=row, column=2, value=period)
        for col, name in enumerate(columns, start=3):
            text = cell_text(name, slot) or (_RESERVED_LABEL if slot in reserved_slots else '')
            if text:
                cell = ws.cell(row=row, column=col, value=text)
                cell.alignment = Alignment(horizontal='center')
    ws.freeze_panes = 'C2'
```

`export_excel(solution, dataset, path, cfg=None)` 里两处 `_write_grid(...)` 调用都加 `dataset.calendar` 参数：

```python
    _write_grid(
        ws, dataset.classes,
        lambda class_id, slot: '/'.join(...),
        dataset.calendar,
        reserved_slots=reserved)

    ws2 = wb.create_sheet('教师课表')
    _write_grid(
        ws2, sorted(dataset.teachers),
        lambda teacher, slot: '/'.join(...),
        dataset.calendar)
```

`_template_cell(class_id, slot)` 签名加 `calendar` 参数：

```python
def _template_cell(class_id, slot, calendar):
    day, period = calendar.slot_of(slot)
    row = _TEMPLATE_FIRST_CLASS_ROW + (class_id - 1)
    col = _TEMPLATE_FIRST_DAY_COL + day * calendar.periods_per_day + (period - 1)
    return row, col
```

`export_to_template(solution, dataset, template_path, out_path, sheet_name='下学期', cfg=None)` 里：

```python
    by_cell = defaultdict(list)
    for p in solution.placements:
        by_cell[_template_cell(p.class_id, p.slot, dataset.calendar)].append(p)
```

以及家族单双周备注列那段：

```python
        note_col_of = {family: _TEMPLATE_FIRST_DAY_COL + len(dataset.calendar.days) * dataset.calendar.periods_per_day + i
                      for i, family in enumerate(families)}
```

确认无遗漏后删掉顶部 `from . import calendar as cal`。

- [ ] **Step 3: 跑测试确认通过**

Run: `python -m pytest tests/test_exporter_template.py tests/test_solve_e2e.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add scheduler/core/exporter.py
git commit -m "refactor: exporter.py 按年级日历导出网格与模板列偏移"
```

---

## Task 9: `cli.py` 按年级日历打印报告

**Files:**
- Modify: `scheduler/cli.py`
- Test: `tests/test_cli_import.py`

- [ ] **Step 1: 确认现有测试作为回归基线**

Run: `python -m pytest tests/test_cli_import.py -v`
Expected: 全部 PASS

- [ ] **Step 2: 替换所有 `cal.` 引用**

`_fmt_slots(slots, calendar)` 签名加 `calendar` 参数：

```python
def _fmt_slots(slots, calendar):
    """{(0,4),(0,5),(2,1)} -> '周一 4,5 | 周三 1'"""
    by_day = OrderedDict()
    for day, period in sorted(slots):
        by_day.setdefault(calendar.days[day], []).append(period)
    return ' | '.join('%s %s' % (d, ','.join(str(p) for p in ps))
                      for d, ps in by_day.items())
```

`_echo_column(rows, column, parser)` 签名加 `calendar` 参数，`parser(raw)` 改成 `parser(raw, calendar)`（因为 Task 3 之后 `parse_time_expr`/`parse_fixed_slots` 都要求两个参数了）：

```python
def _echo_column(rows, column, parser, calendar):
    """按原文去重，输出「原文 -> 解析结果」对照。"""
    lines = []
    seen = set()
    for row in rows:
        raw = row[COLUMNS[column]]
        raw = '' if raw is None else str(raw).strip()
        if not raw or raw in seen:
            continue
        seen.add(raw)
        parsed = parser(raw, calendar) if parser in (parse_time_expr, parse_fixed_slots) else parser(raw)
        if isinstance(parsed, set):
            rendered = _fmt_slots(parsed, calendar)
        else:
            rendered = '; '.join('%s %s' % (f['type'], f['params']) for f in parsed)
        lines.append('  %s -> %s' % (raw, rendered))
    return lines
```

（`parse_requirement`/`parse_remark` 签名没变，还是一个参数——上面这行用 `parser in (parse_time_expr, parse_fixed_slots)` 区分，避免给不需要 calendar 的两个解析函数也传参数报错。）

`render_import_report(result, rows=None)`：

```python
def render_import_report(result, rows=None) -> str:
    ds = result.dataset
    calendar = ds.calendar
    used = {}
    for task in ds.tasks:
        if task.consumes_slot:
            used[task.class_id] = used.get(task.class_id, 0) + task.periods
    occupancy = sorted(set(used.values()))

    out = [
        '=== 导入概览（%s）===' % ds.grade,
        '教师 %d 人 | 班级 %d 个 | 任务 %d 个（占格 %d 个）'
        % (len(ds.teachers), len(ds.classes), len(ds.tasks),
           sum(1 for t in ds.tasks if t.consumes_slot)),
        '每班占格数 %s / 每周 %d 格' % (occupancy, calendar.n_slots),
        '生成规则 %d 条' % len(result.rules),
        '',
    ]
    if rows is not None:
        out.append('=== 中文规则解析回显（请逐条核对）===')
        for column, parser in [('不能排课节次', parse_time_expr),
                               ('固定节次', parse_fixed_slots),
                               ('排课要求', parse_requirement),
                               ('备注', parse_remark)]:
            out.append(column + '：')
            out.extend(_echo_column(rows, column, parser, calendar))
```

（保留后续未展示的代码不变；这里只是把 `cal.N_SLOTS` 换成 `calendar.n_slots`，`_echo_column` 调用加 `calendar` 参数。）

顶部 `from .core import calendar as cal` 确认没有其他用处后删除。

- [ ] **Step 3: 跑测试确认通过**

Run: `python -m pytest tests/test_cli_import.py -v`
Expected: PASS

- [ ] **Step 4: 跑全量测试**

Run: `python -m pytest -q`
Expected: PASS，这是最后一个消费方文件，全绿代表整个迁移完成

- [ ] **Step 5: Commit**

```bash
git add scheduler/cli.py
git commit -m "refactor: cli.py 按年级日历渲染导入报告"
```

---

## Task 10: 用七年级/八年级真实数据验证参数化真的生效

**Files:**
- Modify: `scheduler/config/calendars.yaml`
- Test: `tests/test_solve_e2e.py`

**Interfaces:**
- Consumes: 前面全部 9 个任务的成果

这一步是整个计划里唯一"新增行为"而不是"保持行为不变的重构"的任务——前面 9 个任务全部只是把九年级/初三这一份数据从硬编码搬到可配置，行为必须零变化；这一步加两个 `periods_per_day` 不同的新年级，第一次真正验证"参数化"这件事本身是对的。

- [ ] **Step 1: 在 `calendars.yaml` 加七年级/八年级数据**

在 `scheduler/config/calendars.yaml` 的 `grade_calendars` 下面追加（真实作息表数据，来自 `作息表模板.xlsx`）：

```yaml
  七年级:
    days: [周一, 周二, 周三, 周四, 周五]
    periods_per_day: 8
    midday_break_after: 4
    clock_times:
      - ["08:25", "09:05"]
      - ["09:20", "10:00"]
      - ["10:15", "10:55"]
      - ["11:10", "11:50"]
      - ["14:20", "15:00"]
      - ["15:15", "15:55"]
      - ["16:10", "16:50"]
      - ["16:50", "17:30"]
    reserved_slots: []

  八年级:
    days: [周一, 周二, 周三, 周四, 周五]
    periods_per_day: 9
    midday_break_after: 5
    clock_times:
      - ["08:00", "08:35"]
      - ["08:50", "09:25"]
      - ["09:55", "10:30"]
      - ["10:45", "11:20"]
      - ["11:35", "12:10"]
      - ["14:20", "15:00"]
      - ["15:15", "15:55"]
      - ["16:10", "16:50"]
      - ["16:50", "17:30"]
    reserved_slots: []
```

- [ ] **Step 2: 写失败的测试**

在 `tests/test_solve_e2e.py` 加一个用七年级日历（8 节而不是 9 节）跑一次最小求解的测试，不依赖真实 Excel（现有 e2e 测试固定用初三真实数据，这里手写一个小数据集验证 8 节的年级也能正常走完整条链路）：

```python
def test_solve_respects_a_different_periods_per_day():
    """七年级 8 节课，不是全局默认的 9 节——验证参数化真的在起作用，
    不是巧合让默认值继续蒙混过关。"""
    from scheduler.core.config import load_config
    from scheduler.core.models import Dataset, Teacher, TeachingTask
    from scheduler.core.solver import solve
    from pathlib import Path

    CONFIG_DIR = Path(__file__).resolve().parents[1] / 'scheduler' / 'config'
    cfg = load_config(CONFIG_DIR)
    calendar = cfg.calendar_of('七年级')
    assert calendar.periods_per_day == 8

    tasks = [TeachingTask(id=0, grade='七年级', class_id=1, course='语文',
                          teacher='张老师', periods=8)]
    dataset = Dataset(grade='七年级', classes=[1],
                      teachers={'张老师': Teacher(name='张老师')},
                      tasks=tasks, calendar=calendar)
    solution = solve(dataset, cfg, [], max_seconds=10)
    assert solution.feasible
    # 8 节全排满一个班的语文，只有 8 节/天 × 5 天 = 40 格可用，8 节远小于上限，
    # 但关键断言是：所有落点的 period 必须 <= 8，如果参数化没生效、
    # 编译器还在用全局 9 节，可能会把任务排到第 9 节（这个年级根本没有）。
    for p in solution.placements:
        _, period = calendar.slot_of(p.slot)
        assert period <= 8
```

- [ ] **Step 3: 跑测试确认失败或通过**

Run: `python -m pytest tests/test_solve_e2e.py -k different_periods -v`
Expected: 如果 Task 5-7 都做对了，这一步应该**直接 PASS**——本任务不改生产代码，只加数据和测试，是对前面 9 个任务的验收。如果失败，说明前面某个文件的 `cal.N_SLOTS`/`cal.PERIODS_PER_DAY` 有遗漏没替换干净，回头用 `grep -rn "cal\." scheduler/core/` 排查

- [ ] **Step 4: 全量测试**

Run: `python -m pytest -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add scheduler/config/calendars.yaml tests/test_solve_e2e.py
git commit -m "test: 用七/八年级真实作息数据验证日历参数化在求解链路里生效"
```

---

## 完成后的状态

`calendar.py` 的全局常量还在（作为 `Dataset.calendar` 的默认值来源），但生产路径（`importer.py` 构造的 `Dataset`）全部走 `calendars.yaml` 驱动的按年级日历。三个年级（初三/七年级/八年级）都有真实数据。`export_to_template` 的"一份模板打天下"限制仍然存在，留给下一个计划处理。年级叫法统一（初一/初二/初三 → 七/八/九年级）没有在这次做，是下一个独立、机械的收尾任务。
